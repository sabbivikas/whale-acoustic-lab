# The public static frontend requires no environment variables.
#
# Researchers may explicitly enter their own compatible HTTPS backend URL in
# the Advanced panel. That URL stays in their browser localStorage; it is not a
# build-time setting and this project never requests a backend API key.
#
# Never add OpenAI, Modal, Vercel, GitHub, Hugging Face, or other credentials
# to frontend environment files.
