# Public frontend configuration only. This URL is embedded in the Vite bundle.
# Never add OPENAI_API_KEY, Modal credentials, or other secrets here.
VITE_WHAM_API_URL=https://your-public-modal-endpoint.modal.run
